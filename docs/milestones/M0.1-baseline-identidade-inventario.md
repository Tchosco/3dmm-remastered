# M0.1 — Baseline, identidade e inventário

**Status:** DONE

## Objetivo

Confirmar e documentar o estado inicial do fork antes de qualquer alteração funcional, estabelecendo um ponto de referência técnico e legal para o Movie Studio Web.

## Repositórios

- Upstream: `Phantomcrew-de/3dmm-remastered`
- Fork: `Tchosco/3dmm-remastered`
- Branch de trabalho: `feature/m0-foundation`
- Branch estável: `main`

## Identidade provisória

Nome de trabalho: **Movie Studio Web**.

Nesta submilestone, o nome é apenas documental. Não houve troca de marca, remoção de créditos ou alteração visual do editor.

## Baseline técnico conhecido

O projeto é um editor web baseado em Three.js, executado no navegador e servido por HTTP local. A base atual contém, entre outros elementos:

- `3dmm-editor.html` — interface principal;
- `3dmm-editor-main.mjs` — lógica principal do editor;
- `render-engine.mjs` — inicialização/renderização Three.js;
- `editor-state.mjs` — estado compartilhado;
- `media.mjs` — helpers de áudio/WAV;
- `ui.mjs` — interface e internacionalização;
- `depth.mjs` — tratamento de profundidade;
- `Backgrounds/` — biblioteca de backgrounds;
- assets de personagem/animação de demonstração;
- arquivos JSON de templates de diálogo/foley.

## Capacidades já identificadas no upstream

- renderização Three.js/WebGL;
- importação de personagem GLB;
- posicionamento, rotação e escala de personagens;
- reprodução e transição de animações;
- timeline com keyframes e playhead;
- gravação de ações na timeline;
- backgrounds com depth map;
- controles de câmera e parâmetros de matchmove;
- áudio e decodificação WAV;
- lipsync experimental;
- estrutura de cenas/takes no estado;
- importação/exportação de projeto `.3dmmproj`;
- opção de exportação/render na interface.

Esta lista será validada funcionalmente em maior profundidade na M0.2. A presença em código/interface não conta, por si só, como confirmação de funcionamento.

## Baseline legal

O código do projeto está sob licença MIT conforme o arquivo `LICENSE` do fork. O aviso existente informa que o projeto não é afiliado ou endossado pela Microsoft e que 3D Movie Maker e seus assets originais pertencem à Microsoft.

Regras para o fork:

1. preservar o arquivo `LICENSE` e avisos existentes;
2. não remover atribuição do upstream;
3. não tratar assets proprietários da Microsoft como parte reutilizável do Movie Studio Web;
4. para qualquer baseline que venha a ser distribuído/publicado, usar preferencialmente assets próprios, de domínio público ou sob licença compatível;
5. manter separação documental entre código MIT e conteúdo/asset de terceiros.

## Execução local validada

Procedimento utilizado:

```bash
python -m http.server
```

Editor acessado em:

```text
http://localhost:8000/3dmm-editor.html
```

## Resultado do smoke test local

Validação humana executada em 2026-09-05.

- [x] servidor HTTP inicia sem erro;
- [x] editor abre no navegador;
- [x] nenhuma falha crítica de carga foi reportada durante o smoke test;
- [x] background padrão aparece;
- [x] personagem de demonstração carrega;
- [x] personagem pode ser selecionado e deslocado;
- [x] menu de ações/animações funciona e animações podem ser acionadas;
- [x] REC pode ser armado/iniciado;
- [x] timeline registra e reproduz a ação testada;
- [x] projeto pode ser exportado em `.3dmmproj`;
- [x] projeto exportado pode ser reimportado;
- [x] cena/timeline permanecem coerentes após reimportação.

## Limitação observada no baseline

### Movimento sem animação locomotiva automática

Durante o teste, o personagem pode ser deslocado normalmente, porém permanece visualmente em pose/animação parada durante o deslocamento, em vez de acionar automaticamente uma animação de caminhada/corrida.

Classificação nesta fase: **limitação funcional conhecida do baseline, não bloqueante para M0.1**.

Não foi corrigida na M0.1. A relação entre deslocamento, detecção de velocidade e animação locomotiva será auditada na M0.2 antes de qualquer alteração de código.

## Entregáveis da M0.1

- [x] fork confirmado;
- [x] branch de trabalho criada;
- [x] plano da M0 criado;
- [x] proveniência/licença registradas;
- [x] inventário inicial de arquivos e capacidades registrado;
- [x] baseline local validado no VS Code/navegador;
- [x] limitações encontradas registradas;
- [x] gate humano da M0.1.

## Gate M0.1

**APROVADA NO GATE HUMANO — DONE em 2026-09-05.**

O fluxo mínimo abrir → manipular → gravar → reproduzir → exportar → reimportar foi confirmado. A limitação de deslocamento sem animação locomotiva automática permanece registrada para investigação na M0.2.
